STATE function
    flag = False
    for statement in STATEMENTS:
        if flag == True:
            print("Dead code statement in",FUNCTION_NAME,":",statement)
        if 'return ' in statement[0:7]:
            flag = True
END_STATE

STATE if
    flag = False
    for statement in STATEMENTS:
        if flag == True:
            print("Dead code statement in if statement at line",LINE_NO,":",statement)
        if 'return ' in statement[0:7]:
            flag = True
END_STATE