# LENGTH NAME
# IF NAME_len >= 31
#     PRINT(line)
#     VIOLATION Variable has length greater than 31
# END_IF

# state: assign
# variables: var,rhs
# logic: 
# - len(var)<5
# - type(rhs) = int
# - check(binary_search)


-------------NEW IDEAS------------

Replace "GREATER THAN" in guidelines with > in makeParser. And many moree..

Write a function that returns all direct variables declared under that rule and all the direct function calls made under that rule and all condition statements etc etc.
     ---- we could create two functions. One that does the above and one that just returns all variables etc(either direct or indirect).
